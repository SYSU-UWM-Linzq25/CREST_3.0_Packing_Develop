function [forcNameInt,subName]=GenerateIntForcingFileName(dirIntForc,fmtSub,fmtInt,date,date0)
subName=['s',datestr(date,fmtSub)];
strFileInt = datestr(date,fmtInt);
if exist('date0','var')
    strFileInt0=datestr(date0,fmtInt);
    [path,~,~]=fileparts(strFileInt0);
else
    [path,~,~]=fileparts(strFileInt);
end
[~,name,~]=fileparts(strFileInt);
forcNameInt=[dirIntForc,path,name,'.mat'];
end