function dispose(this,coreList)
LocalDir=['/work',this.forcingVar.pathSplitor];
for i=1:length(list)
    notifiedName=[LocalDir,num2str(coreList(i)),'.finished'];
    while exist(notifiedName,'file')~=2
        pause(2);
    end
end
delete([LocalDir,'*.finished']);
rmdir
end